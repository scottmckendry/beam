package views

templ Root(isAdmin bool) {
	@BaseLayout() {
		if !isAdmin {
			@NonAdmin()
		} else {
			<h1 class="text-2xl font-bold mb-4">Welcome to Beam!</h1>
			<p class="mb-4">You are an admin user.</p>
		}
	}
}
